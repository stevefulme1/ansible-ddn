---
argument_specs:
  main:
    short_description: "Set up LustrePerfMon and alerting"
    options:
      ddn_monitoring_host:
        description: Target host address
        type: str
        required: true
      ddn_monitoring_username:
        description: Authentication username
        type: str
      ddn_monitoring_password:
        description: Authentication password
        type: str
      ddn_monitoring_api_key:
        description: API key for authentication
        type: str
      ddn_monitoring_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      ddn_monitoring_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
